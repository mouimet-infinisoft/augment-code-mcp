# Memory MCP Configuration
# No configuration is required by default, but you can customize if needed

# Optional: Custom memory storage path
# If not provided, memories will be stored in the default location
# MEMORY_STORAGE_PATH=/path/to/custom/storage

# Optional: Server configuration
# PORT=3000
# HOST=localhost
